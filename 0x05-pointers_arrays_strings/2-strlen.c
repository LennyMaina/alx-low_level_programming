#include "main.h"
#include <string.h>

/**
 *_strlen - function length of a string
 *@n: variable
 *@s: char
 *Return: length
 */
int _strlen(char *s)
{
char s[100];
int n;
_putchar("My first strlen!");
gets(string);
n = strlen(string);
puts("Length = %d", n);
return (n);
}
