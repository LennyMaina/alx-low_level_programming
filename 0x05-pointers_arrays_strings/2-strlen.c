#include "main.h"
#include <string.h>

/**
 *_strlen - function length of a string
 *@l: variable
 *@s: char
 *Return: length
 */
int _strlen(char *s)
{
int len;
for (len = 1; ++len;)
printf("Length = %d", len);
}
